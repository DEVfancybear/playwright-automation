interface:
  display_name: "Playwright QA Automation"
  short_description: "Điều tra trực tiếp trên trình duyệt, tái hiện bug, kiểm thử Playwright khi cần chạy lại"
  default_prompt: "Use $playwright-automation to decode this bug log, preserve its cross-screen flow and timing, and reproduce it with evidence by driving a real browser directly. Only write a Playwright regression test if I ask for one, or if the scenario cannot be done by hand."
