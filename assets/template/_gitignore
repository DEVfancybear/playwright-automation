node_modules/

# Bí mật
.env
.env.local

# Phiên đăng nhập — chứa cookie thật, ai có file này là vào được tài khoản
.auth/

# Artefact của phiên EXPLORE — chứa URL nội bộ, tên tài khoản, đôi khi cả token trong HAR.
# Tài liệu use case và test-plan đáng commit thì chuyển ra docs/ trước, đừng bỏ ignore cả thư mục.
.testagent/

# Kết quả chạy test
test-results/
playwright-report/
blob-report/
playwright/.cache/
allure-results/
allure-report/

# Ảnh baseline của visual test thì PHẢI commit (nằm trong *-snapshots/),
# nhưng ảnh diff sinh ra lúc chạy thì không.
**/*-diff.png
**/*-actual.png
