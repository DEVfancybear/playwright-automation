# Evidence — local MCP authentication bridge for 3.0.1

Date: **2026-08-24**

Implementation snapshot: Git index tree `710026ea6ac5f99301788ede3b5cb23be29e64bc` (before this evidence-only refresh)

Spec: `verification/3.0.1-BRIDGE-SPEC.md`

Spec approval: **not obtained (autonomous run)**

## RED evidence

- Initial bridge suite against not-implemented stubs: **8/8 failed**.
- Custom ARIA role dropdown scenario failed at the login URL before custom option support was implemented.
- The real Playwright MCP init-page call shape test failed because the hook expected `{ page }` instead of `page`.
- The CommonJS init-page adapter test failed with `MODULE_NOT_FOUND` before the adapter was added.

## Upstream compatibility evidence

Command:

```text
npm exec --yes --package=@playwright/mcp@0.0.79 -- playwright-mcp --help
```

Result: exit `0`. The pinned CLI exposes `--extension`, `--init-page <path...>` and `--secrets <path>`. Inspection of the installed pinned package showed that init-page modules are loaded with `require(path)`, their `default` export is read, and the function is invoked as `func(page)`. `scripts/mcp-auth-init.cjs` is the compatibility adapter for that contract.

## GREEN evidence

Focused bridge suite:

```text
npm run test:bridge
tests 9; pass 9; fail 0; cancelled 0; skipped 0
```

Covered behavior:

- wrapper pins `@playwright/mcp@0.0.79` and includes extension/init-page/secrets flags without printing dotenv values;
- missing credentials, unsafe login URLs and inherited `NODE_DEBUG` fail closed;
- exact URL matching rejects hostile origin/path/query/fragment variants;
- configured credential values are treated as opaque and are not rejected by format;
- one-step login, direct upstream init-page signature, custom role dropdown + delayed two-step login, delayed TOTP and lookalike-page rejection.

Mutation evidence:

```text
manual mutation: 5/5 killed
bridge mutation: 5/5 killed
```

Bridge mutants killed: exact URL always matches, role selection skipped, TOTP replaced with a constant, MCP secrets redaction omitted, and `NODE_DEBUG` fail-open.

## Final release gauntlet

Command: `npm run gauntlet`

```text
tests 40; pass 40; fail 0; cancelled 0; skipped 0
manual auth mutation 5/5 killed
manual bridge mutation 5/5 killed
skill description 869 chars
secret/artifact filename gate 67 files checked
package @duong.dev/playwright-automation@3.0.1: 54 files, required payload present
npm audit: 0 vulnerabilities
final staged release state: 18 changed paths, 19 critical files indexed
GAUNTLET_OK
```

`python -X utf8 .../skill-creator/scripts/quick_validate.py <repo>` also returned `Skill is valid!`.

## Scope and residual evidence gap

- No independent sub-agent verification was performed because delegation was not authorized for this run.
- The local integration suite used real Chromium against an isolated HTTP app. It did not connect to the user's Chrome profile, extension token, VPN or live CMS; that final environment-specific check requires installing/configuring the local MCP server and extension on the user's machine.
- Live pre-release checks found Git tag `v3.0.0` and npm registry version `3.0.0`; therefore the release follow-up bumped the patch version to `3.0.1`.
- No npm publish, tag, browser-extension install or Claude MCP config mutation was performed. The user explicitly authorized the commit and branch push after this evidence refresh.
